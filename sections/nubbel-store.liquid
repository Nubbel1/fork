<section class="min-h-screen bg-white p-6">
  <h1 class="text-4xl font-bold text-center mb-10">nubbel</h1>

  <div class="mb-8 p-4 border rounded-xl shadow text-center">
    <h2 class="text-xl font-semibold">🛒 Carrito</h2>
    <p class="text-gray-700 mb-4">El carrito será manejado por Shopify automáticamente</p>
  </div>

  <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
    {% assign products = collections.all.products | slice: 0, 9 %}
    {% for product in products %}
      <div class="border p-4 rounded-2xl shadow hover:shadow-lg transition">
        <img
          src="{{ product.featured_image | img_url: 'medium' }}"
          alt="{{ product.title }}"
          class="w-full h-80 object-cover rounded-xl mb-4"
        />
        <h2 class="text-xl font-semibold mb-2">{{ product.title }}</h2>
        <p class="text-gray-600 text-sm mb-2">{{ product.description | truncate: 100 }}</p>
        <p class="text-lg font-semibold mb-4">{{ product.price | money }}</p>
        <form method="post" action="/cart/add">
          <input type="hidden" name="id" value="{{ product.variants.first.id }}">
          <button
            type="submit"
            class="block w-full bg-black text-white px-4 py-2 rounded-full text-center hover:bg-gray-800 transition"
          >
            Añadir al carrito
          </button>
        </form>
      </div>
    {% endfor %}
  </div>
</section>

